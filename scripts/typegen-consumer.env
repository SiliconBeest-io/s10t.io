# Type generation only (queue consumer). These placeholders are not runtime
# secrets and must never be used as deployed or local-development credential
# values. Set the real value with: wrangler secret put SENTRY_DSN
SENTRY_DSN=typegen-placeholder
